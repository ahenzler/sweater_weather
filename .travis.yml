language: ruby
rbenv:
- 2.7.2
services:
  - postgresql
before_script:
  - bundle config set --local deployment 'true'
  - bundle exec rake db:create
  - bundle exec rake db:migrate
script:
  - bundle exec rspec